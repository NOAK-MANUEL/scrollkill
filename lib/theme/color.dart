import 'package:flutter/material.dart';

class ScrollKillColors {
  // Primary
  static const Color primaryRed = Color(0xFFE53935);

  // Backgrounds
  static const Color background = Color(0xFF0F1115);
  static const Color surface = Color(0xFF161A20);

  // Borders & dividers
  static const Color divider = Color(0xFF242833);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B6C3);
  static const Color textDisabled = Color(0xFF6B7280);

  // Status
  static const Color warning = Color(0xFFFF9800);
  static const Color success = Color(0xFF4CAF50);
  static const Color info = Color(0xFF2196F3);
}
